# notebooks/

One marimo notebook per trial. Naming: `trial-shortname.py` (e.g. `nice-sugar.py`,
`recovery-dexamethasone.py`). Each notebook is standalone with PEP 723 inline
dependencies so `uv run marimo edit notebooks/<file>.py` just works.
